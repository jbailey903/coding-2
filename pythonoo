#python oop-object oreniated programing

# object- a representation
# of some complex data structure 
# data= data types

# class- is a structure/ blueprint for creating
# objects

class computers:
    def _ init_ (self, name,brand,color,shape,storage, portablility, camera, ram, processor):
        self